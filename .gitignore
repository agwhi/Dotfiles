# Sensitive files - .example files are used as templates
gitconfig.local

# Backup directories and files
backups/
symlink-backups.txt
backup-log.txt

# Local migration snapshots and command reports
reports/

# Local personal package manifests. These are intentionally outside the
# development baseline, but can be consumed manually with brew bundle.
system/packages/personal.Brewfile

# APM generated dependency and target output
system/ai/apm/apm_modules/
system/ai/apm/.agents/
system/ai/apm/.codex/
system/ai/apm/.claude/
system/ai/apm/.opencode/
system/ai/apm/AGENTS.md
system/ai/apm/CLAUDE.md

# Python validation/cache artifacts
__pycache__/
*.py[cod]

# macOS Finder noise
.DS_Store

# Accidental literal-tilde directory (unexpanded ~ from a script)
/~/
