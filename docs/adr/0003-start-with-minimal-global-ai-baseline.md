# Start with a Minimal Global AI Baseline

The Global AI Baseline should start with only `grill-with-docs` as a Baseline AI Asset. We explicitly do not include `using-superpowers` or broader language/framework/domain skills globally at this stage, because project-specific AI Assets should remain in the repos that need them and the baseline should earn expansion through repeated cross-project use.
